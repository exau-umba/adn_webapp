# Base URL du backend (passerelle nginx). Laisser vide en dev pour utiliser le proxy Vite (/api → localhost:8080).
# Exemple si le service users est exposé directement (pensez à activer CORS côté Django) :
# VITE_API_BASE_URL=http://localhost:8000

VITE_API_BASE_URL=
